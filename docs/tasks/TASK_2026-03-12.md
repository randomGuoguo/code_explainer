# TASK_2026-03-12

## 0. 本次目标
- 为 `code_explainer.py` 增加“指定 Markdown 输出目录”能力
- 为 `code_explainer.py` 增加“删除全部函数/指定函数 docstring”能力
- 补齐对应测试与测试总表记录

## 1. 本次完成情况
- 已支持 `--md-out-dir DIR`
  - 生成的 Markdown 文件名仍为 `<stem>_doc.md`
  - 输出目录不存在时会自动创建
- 已支持删除模式
  - `--remove-docstrings-all`
  - `--remove-docstrings foo Cls.bar`
  - `--emit-md-when-removing`
- 删除模式默认只修改 `.py`，不生成 Markdown
- 已更新 `README.md` 的 CLI 示例
- 已新增设计文档与实现计划
  - `docs/plans/2026-03-12-md-output-remove-docstrings-design.md`
  - `docs/plans/2026-03-12-md-output-remove-docstrings.md`

## 2. 关键实现点
- 在 `code_explainer.py` 中新增：
  - `remove_docstrings()`
  - `resolve_md_output_path()`
  - `_get_docstring_expr()`
- 扩展 `run_on_file()`，支持：
  - `generate` 模式
  - `remove` 模式
- 扩展 `main()` CLI 参数：
  - `--md-out-dir`
  - `--remove-docstrings-all`
  - `--remove-docstrings`
  - `--emit-md-when-removing`

## 3. 测试情况
- 已更新 `tests/test_code_explainer.py`
- 已新增 `tests/tests_process.md` 测试总表
- 本次执行并通过：
  - `python -m pytest -q tests/test_code_explainer.py`
  - `python -m pytest -q`
- 当前结果：`19 passed`

## 4. 涉及文件
- `code_explainer.py`
- `tests/test_code_explainer.py`
- `tests/tests_process.md`
- `README.md`
- `docs/plans/2026-03-12-md-output-remove-docstrings-design.md`
- `docs/plans/2026-03-12-md-output-remove-docstrings.md`

## 5. 当前注意事项
- 当前工作区除本次改动外，仍存在其它未提交变更；本次未回退这些内容
- 删除模式下若指定不存在的 qualname，会打印 warning，但不会中断处理
- 当前仍只处理模块顶层函数与类方法，不处理嵌套函数/嵌套类
